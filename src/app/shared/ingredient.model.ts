export class Ingredient {
  // public name: string;
  // public amount: number;
  // constructor(name: string, amount: number) {
  //   this.name = name;
  //   this.amount = amount;
  // }
  // OTHER OPTION WITH ACCESOR FROM TYPESCRIPT SAME EFFECT AS BEFORE CREATES AND ASSIGN PROPERTIES
  constructor(public name: string, public amount: number) {}
}
